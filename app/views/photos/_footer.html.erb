<%= render partial: 'users/info', locals: { user: photo.user } %>
<% if current_user_can_edit?(photo) %>
  <%= button_to '', event_photo_path(photo.event, photo),
                method: :delete,
                form: { data: { turbo_confirm: t('views.photos.footer.confirm_deletion') } },
                class: 'btn bi bi-trash2',
                form_class: 'd-inline',
                style: 'color: white'
  %>
<% end %>
