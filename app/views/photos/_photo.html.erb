<% image_title = "#{render partial: 'photos/footer', locals: { photo: photo }}"%>

<%= link_to image_tag(photo.attached_photo.variant(:thumb), class: 'img-fluid'),
            photo.attached_photo.url,
            data: { lightbox: 'lightbox-photos', title: image_title }
    %>
